/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["technext.github.io", "images.pexels.com","upload.wikimedia.org"],
  },
};

module.exports = nextConfig
