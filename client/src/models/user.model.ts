// Re-exported from shared — single source of truth
export type { IUser } from '@shared/types/user.types';
// Legacy alias kept for backward compatibility
export type { IUser as UserModel } from '@shared/types/user.types';
