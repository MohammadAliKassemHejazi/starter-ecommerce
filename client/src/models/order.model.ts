// Re-exported from shared — single source of truth
export type { IOrder, IOrderItem } from '@shared/types/order.types';
// Legacy alias kept for backward compatibility
export type { IOrder as IOrderModel } from '@shared/types/order.types';
