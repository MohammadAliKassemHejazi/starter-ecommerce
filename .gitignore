graphify-out/
.claude/company/.teamlead.lock
node_modules/
.env
.env.*
!.env.example
!.env.template
.claude/setup/.snapshots/*
!.claude/setup/.snapshots/.gitkeep
.claude/company/.last-optimization-review
.claude/settings.local.json
repomix-output.xml

# --- Added during repo audit: artifact classes that were being committed by accident ---
# Runtime / build output
*.log
logs/
server/dist/
client/.next/
client/out/
client/test-results/
client/playwright-report/
coverage/

# Stray local database files (SQLite artifacts from test runs)
*.sqlite
*.sqlite3
*.db
server/127.0.0.1

# Screenshots dumped at the repo root while debugging
/*.png

# Tool-generated backups
*.graphify-bak

# Claude Code specialist worktrees (transient runtime state, not repo content —
# each has its own .git worktree link to an absolute local path)
.claude/worktrees/

# Installer-proposed graphify skill directory — unreviewed, pending Founder
# approval per guardrail 8 (external tool boundaries). See
# .claude/setup/.snapshots/ and RESUME-POINT.md for the pending decision.
.claude/skills/graphify/
